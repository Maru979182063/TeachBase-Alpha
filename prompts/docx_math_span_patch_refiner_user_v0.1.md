Repair only the listed risky spans.

Patch rules:
- Use `task_id` to identify the span. Do not return `find_text`.
- `source_span` is the exact source substring selected by local code.
- `replacement_text` must preserve the same mathematical meaning.
- Prefer the smallest replacement that fixes rendering.
- Do not rewrite surrounding explanation prose.
- Do not include `render_markdown`; it is generated by local code.

Input:
```json
{{input_json}}
```

Required output:
```json
{
  "schema": "docx_math_span_patch_actions_v0.2",
  "draft_id": "{{draft_id}}",
  "source_group_id": "{{source_group_id}}",
  "patches": [
    {
      "task_id": "sp_0001",
      "replacement_text": "",
      "confidence": "high|medium|low",
      "notes": ""
    }
  ],
  "unresolved": [
    {
      "task_id": "",
      "reason": ""
    }
  ]
}
```
