{
  "schema_version": "pipeline_feature_flags.v0.1",
  "defaults_are_safe": true,
  "flags": {
    "semantic_role_adapter_shadow": {
      "default": false,
      "owner": "pipeline_control_plane",
      "description": "Future Semantic Role Adapter shadow sidecar switch. Not wired in this branch."
    },
    "semantic_visual_assignment_experiment": {
      "default": false,
      "owner": "pipeline_control_plane",
      "description": "Future guard for visual semantic assignment experiments. Not wired in this branch."
    },
    "docx_native_ingest_enabled": {
      "default": false,
      "owner": "pipeline_control_plane",
      "description": "DOCX native-first ingest v0.1 switch. Default false; the pipeline can generate preview artifacts but does not import Runtime/DB by default."
    },
    "english_text_first_enabled": {
      "default": false,
      "owner": "pipeline_control_plane",
      "description": "Future English image PDF text-first switch. Not implemented in this branch."
    },
    "english_visual_fallback_enabled": {
      "default": false,
      "owner": "pipeline_control_plane",
      "description": "Future English page-level visual fallback switch. Not implemented in this branch."
    },
    "runtime_import_enabled": {
      "default": false,
      "owner": "runtime_backend",
      "description": "Control-plane declaration only; existing runtime behavior is unchanged by this file."
    }
  }
}
