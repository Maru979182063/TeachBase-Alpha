# 拆分构建首轮样例报告 v0.1

时间：2026-06-16  
样本：高一数学暑假《第6讲 基本不等式》教师版/学生版  
目标：验证“基底结构 + 学科插件 + 教师/学生对齐 + 题目/答案/解析绑定”的首个闭环。

## 1. 本轮已经构建的内容

已新增拆分构建目录：

`C:\Users\EDY\Documents\教研基建\split_builder`

核心文件：

| 文件 | 作用 |
|---|---|
| `split_builder\schemas\lesson_split.schema.json` | 统一输出结构 |
| `split_builder\config\base_tree.json` | 全学科课堂基底结构 |
| `split_builder\config\plugins\math.json` | 数学插件雏形 |
| `split_builder\prototype_splitter.py` | 首版样本拆分脚本 |

样例输出目录：

`C:\Users\EDY\Documents\教研基建\outputs\split_builder\math_basic_inequality`

输出文件：

| 文件 | 作用 |
|---|---|
| `lesson_split.json` | 可供工作台读取的结构化拆分结果 |
| `review_summary.md` | 给人工/教研快速复核的摘要 |

## 2. 样例拆分结果

| 项目 | 结果 |
|---|---:|
| 教师版页数 | 19 |
| 学生版页数 | 8 |
| 识别考点数 | 5 |
| 识别题块数 | 40 |
| 教师/学生对齐题块 | 40 |
| 对齐率 | 100% |

识别出的考点结构：

| 考点 | 题块数 |
|---|---:|
| 考点 1：不等式的直接应用 | 9 |
| 考点 2：配凑法求最值 | 9 |
| 考点 3：常数代换法求最值 | 6 |
| 考点 4：消元法求最值 | 6 |
| 考点 5：整体化求最值 | 10 |

## 3. 当前数据结构能表达什么

每道题现在已经能表达：

```text
task_id
section_id
question_no
student_stem
teacher_stem
answer
answer_raw
answer_status
explanation
source_pages
difficulty_tier
support_level
transfer_level
visibility
alignment_status
risk_flags
```

这意味着工作台可以同时看到：

- 学生版题干。
- 教师版原题干。
- 规范化答案。
- 原始答案文本。
- 解析/分析/详解。
- 原 PDF 页码。
- 基础/普通/培优初判。
- 是否有公式、图像、证明题等风险。
- 教师/学生是否成功对齐。

## 4. 本轮暴露并已调整的插件点

第一处：`任意实数` 不能直接判为培优。  
原本数学插件把“任意”作为高阶信号，会把“不等式对任意实数成立”误判成 advanced。本轮已收紧规则，保留 `参数、恒成立、综合、求证、证明` 等更可靠信号。

第二处：选择题答案需要 `answer_raw` 和 `answer` 分离。  
PDF 文本层有时会把公式碎片混到 `【答案】` 后，例如原始答案里出现额外的 `4 4 4`。现在保留 `answer_raw`，同时把选择题规范化为 `A/B/C/D`。

第三处：不能把“没有显式【答案】”误判为缺答案。  
有些题只有 `【解析】/【解答过程】`，没有单独 `【答案】`。现在用 `answer_status` 区分：

| 状态 | 含义 |
|---|---|
| `explicit_answer` | 有显式答案 |
| `solution_only` | 没有显式答案，但有解析/解答过程 |
| `missing_or_unrecognized` | 暂未识别答案和解析 |

本样例中：

| 答案状态 | 数量 |
|---|---:|
| explicit_answer | 35 |
| solution_only | 5 |
| missing_or_unrecognized | 0 |

## 5. 分层初判结果

| 层级 | 数量 |
|---|---:|
| basic | 3 |
| standard | 33 |
| advanced | 4 |

这个结果只能视为“插件初判”，不能直接作为教研终审。数学后续要把分层规则做得更细：

- 基础：概念辨析、等号成立条件、直接比较。
- 普通：配凑、消元、整体化、常规最值。
- 培优：证明、多条件综合、参数/恒成立、陌生结构。

## 6. 当前准确性判断

本轮“结构拆分”和“题目对齐”表现很好：

- 5 个考点完整识别。
- 40 道题完整识别。
- 教师/学生题块全部对齐。
- 没发现答案串入学生版的结构问题。

但“数学表达质量”仍需插件继续增强：

- PDF 文本层里的公式符号仍有异常字符。
- 复杂公式需要图像资产或公式识别插件进一步还原。
- 难度标签还只是规则初判，需要模型结合题目语义复核。
- 没有显式答案的题，需要模型从解析中抽取最终答案或标记人工处理。

## 7. 下一步构建建议

建议拆分构建继续按“一个主流程 + 多插件”推进：

第一步：强化数学插件  
把题型识别细化为：概念辨析、选择题、填空题、证明题、最值题、函数型最值、综合题。

第二步：接一个模型复核层  
规则先拆边界，模型再判断：

```text
题型
知识点
分层
是否需要人工审核
是否需要公式/图像复核
```

第三步：复制到第二个学科  
推荐下一个样本用化学，因为化学能验证方程式、实验、答案解析边界，复杂度比数学高一档。

第四步：再接语文样本  
语文用于验证长文本、题源、答案采分点、范文和评分标准。它会逼出工作台的人工审核设计。

## 8. 本轮结论

首轮样例证明：现有讲义具备“精准拆出基底结构和题目”的条件。  
当前最合理的工程方向是：

```text
规则负责稳定边界
模型负责语义打标
插件负责学科差异
人工负责最终审核
```

这条路线可以继续往可编辑工作台推进。

