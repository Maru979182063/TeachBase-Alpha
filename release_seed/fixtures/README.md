# Fixtures

`minimal_valid/` is a deterministic, synthetic package used only to test the
Release Seed contract. It is not sourced from production material, is not a
Release Seed candidate, and must not be imported as real content.

Rebuild it with:

```powershell
python release_seed/fixtures/build_fixtures.py
```
